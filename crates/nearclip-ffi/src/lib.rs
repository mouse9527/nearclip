//! NearClip FFI Module
//!
//! Foreign Function Interface bindings using uniffi.
//! Exports NearClip API for Swift (macOS/iOS) and Kotlin (Android).

#![allow(dead_code)]
#![allow(unused_variables)]

// Note: uniffi::setup_scaffolding!() will be enabled in Story 4-1 (uniffi-swift-bindings)
// when the .udl interface definition file is created.
// See: https://mozilla.github.io/uniffi-rs/

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert!(true);
    }
}
